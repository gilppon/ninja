rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // 유저는 자신의 문서만 읽기/쓰기 가능
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // 나머지 문서는 모두 차단
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
